---
figstash-query: "minor:feat"
figstash-cli: "minor:feat"
---

Return referenced styles, derived design values, and component metadata with compact node queries so an Agent can consume a self-contained offline design-to-code context.
